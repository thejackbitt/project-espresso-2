<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Espresso.CustomActionBar"
             x:Name="this">
    <VerticalStackLayout Spacing="0" Background="{StaticResource NavBarBackground}">
        <Grid VerticalOptions="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
        </Grid>

        <!-- actual loader menu -->
        <StackLayout IsVisible="False" x:Name="loaderPanel" TranslationY="-200">
            <Button Text="Load File" 
                    Margin="20,5"/>
        </StackLayout>

    </VerticalStackLayout>

    <ContentView.Resources>
        <ResourceDictionary>
            <LinearGradientBrush x:Key="NavBarBackground" StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#21AF74" Offset="0.0" />
                <GradientStop Color="#257072" Offset="1.0" />
            </LinearGradientBrush>
        </ResourceDictionary>
    </ContentView.Resources>
</ContentView>
